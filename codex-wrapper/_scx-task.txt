In this file scx-agentic-shim.mjs, the upstream SCX /models endpoint returns JSON shaped {"data":[ ...model objects... ]} but the codex CLI expects {"models":[ ...same objects... ]} and logs "failed to refresh available models: missing field `models`". Fix ONLY the shim: when it proxies the GET request whose path ends in /models, remap the upstream response body from {"data":[...]} to {"models":[...]} (keep every model object exactly as-is; if there is no data field, pass through unchanged). Do not change any other behaviour. After editing, print the exact unified diff of your change and a one-line summary.
