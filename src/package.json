{
  "name": "SCXCode",
  "displayName": "Kritical SCXCode",
  "description": "SCX in VS Code the Kritical way — chat + inline complete + model picker + auto-failover for MiniMax-M2.7 / MAGPiE / gpt-oss-120b / DeepSeek-V3.1 / coder / gemma-4 / Qwen3 / Llama-4-Maverick.",
  "version": "0.1.27",
  "publisher": "kritical",
  "author": {
    "name": "Joshua Finley",
    "email": "joshua.finley@kritical.net"
  },
  "license": "Apache-2.0",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "AI",
    "Chat",
    "Machine Learning",
    "Programming Languages",
    "Other"
  ],
  "keywords": [
    "ai",
    "scx",
    "chat",
    "kritical",
    "anthropic",
    "claude",
    "minimax",
    "gpt-oss",
    "deepseek",
    "llm",
    "completion"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "kritical.scxcode.openChat",
        "title": "Kritical: Open SCX Chat",
        "category": "Kritical SCXCode",
        "icon": {
          "light": "media/kritical-symbol.png",
          "dark": "media/kritical-symbol.png"
        }
      },
      {
        "command": "kritical.scxcode.pickModel",
        "title": "Kritical: Pick SCX Model",
        "category": "Kritical SCXCode"
      },
      {
        "command": "kritical.scxcode.testConnection",
        "title": "Kritical: Test SCX Connection (ping /v1/messages)",
        "category": "Kritical SCXCode"
      },
      {
        "command": "kritical.scxcode.showStatus",
        "title": "Kritical: Show SCX Status (models / quota / fallback chain)",
        "category": "Kritical SCXCode"
      },
      {
        "command": "kritical.scxcode.explainSelection",
        "title": "Kritical: Explain Selected Code",
        "category": "Kritical SCXCode"
      },
      {
        "command": "kritical.scxcode.refactorSelection",
        "title": "Kritical: Refactor Selection (Kritical style)",
        "category": "Kritical SCXCode"
      },
      {
        "command": "kritical.scxcode.auditDiff",
        "title": "Kritical: Audit Current Diff vs CLAUDE.md HARD RULES",
        "category": "Kritical SCXCode"
      },
      {
        "command": "kritical.scxcode.newChat",
        "title": "Kritical: New Chat",
        "category": "Kritical SCXCode",
        "icon": "$(add)"
      },
      {
        "command": "kritical.scxcode.openSettings",
        "title": "Kritical: Open SCXCode Settings",
        "category": "Kritical SCXCode",
        "icon": "$(gear)"
      },
      {
        "command": "kritical.scxcode.manageMcp",
        "title": "Kritical: Manage MCP Servers & Tools",
        "category": "Kritical SCXCode",
        "icon": "$(plug)"
      },
      {
        "command": "kritical.scxcode.explainFile",
        "title": "Kritical: Explain Active File",
        "category": "Kritical SCXCode"
      },
      {
        "command": "kritical.scxcode.generateTests",
        "title": "Kritical: Generate Tests for Active File",
        "category": "Kritical SCXCode"
      },
      {
        "command": "kritical.scxcode.muxQuery",
        "title": "Kritical: Synthetic-Context Query (mux N streams)",
        "category": "Kritical SCXCode"
      },
      {
        "command": "kritical.scxcode.lookingGlass",
        "title": "Kritical SCX: Open Lens Looking Glass",
        "category": "Kritical SCXCode",
        "icon": "$(telescope)"
      },
      {
        "command": "kritical.scxcode.scxCodex",
        "title": "Kritical: Open SCX Codex (terminal — SCX-branded, never touches real codex)",
        "category": "Kritical SCXCode"
      },
      {
        "command": "kritical.scxcode.checkUpdate",
        "title": "Kritical: Check for SCXCode Update (sideloaded VSIX)",
        "category": "Kritical SCXCode"
      },
      {
        "command": "kritical.scxcode.setupGui",
        "title": "Kritical: SCX Setup (Codex options + MCP servers + SCXCode — shared config)",
        "category": "Kritical SCXCode",
        "icon": "$(settings-gear)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "kritical.scxcode.newChat",
          "when": "view == kritical.scxcode.chat",
          "group": "navigation@1"
        },
        {
          "command": "kritical.scxcode.manageMcp",
          "when": "view == kritical.scxcode.chat",
          "group": "navigation@2"
        },
        {
          "command": "kritical.scxcode.openSettings",
          "when": "view == kritical.scxcode.chat",
          "group": "navigation@3"
        },
        {
          "command": "kritical.scxcode.scxCodex",
          "when": "view == kritical.scxcode.chat",
          "group": "navigation@4"
        },
        {
          "command": "kritical.scxcode.lookingGlass",
          "when": "view == kritical.scxcode.chat",
          "group": "navigation@5"
        },
        {
          "command": "kritical.scxcode.setupGui",
          "when": "view == kritical.scxcode.chat",
          "group": "navigation@6"
        }
      ],
      "editor/title": [
        {
          "command": "kritical.scxcode.openChat",
          "group": "navigation@99",
          "when": "true"
        }
      ],
      "editor/context": [
        {
          "when": "editorHasSelection",
          "command": "kritical.scxcode.explainSelection",
          "group": "kritical@1"
        },
        {
          "when": "editorHasSelection",
          "command": "kritical.scxcode.refactorSelection",
          "group": "kritical@2"
        }
      ],
      "commandPalette": [
        {
          "command": "kritical.scxcode.openChat",
          "when": "true"
        },
        {
          "command": "kritical.scxcode.pickModel",
          "when": "true"
        },
        {
          "command": "kritical.scxcode.testConnection",
          "when": "true"
        },
        {
          "command": "kritical.scxcode.showStatus",
          "when": "true"
        },
        {
          "command": "kritical.scxcode.lookingGlass",
          "when": "true"
        },
        {
          "command": "kritical.scxcode.scxCodex",
          "when": "true"
        },
        {
          "command": "kritical.scxcode.checkUpdate",
          "when": "true"
        },
        {
          "command": "kritical.scxcode.setupGui",
          "when": "true"
        }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "kritical-scxcode",
          "title": "Kritical SCXCode",
          "icon": "$(comment-discussion)"
        }
      ]
    },
    "views": {
      "kritical-scxcode": [
        {
          "type": "webview",
          "id": "kritical.scxcode.chat",
          "name": "Chat",
          "icon": "$(comment-discussion)"
        }
      ]
    },
    "configuration": {
      "title": "Kritical SCXCode",
      "properties": {
        "kritical.scxcode.apiKey": {
          "type": "string",
          "default": "${env:SCX_API_KEY}",
          "description": "SCX API key. Default reads HKCU env SCX_API_KEY (Kritical convention). Set explicitly to override.",
          "markdownDescription": "SCX API key. **Default reads HKCU env `SCX_API_KEY`** (Kritical convention). Set explicitly here to override per-workspace. Never commit the resolved value.",
          "scope": "application"
        },
        "kritical.scxcode.baseUrl": {
          "type": "string",
          "default": "https://api.scx.ai",
          "description": "SCX API base URL. Change only if you point at a self-hosted SCX mirror.",
          "scope": "application"
        },
        "kritical.scxcode.codexWrapperPath": {
          "type": "string",
          "default": "",
          "description": "Absolute path to the SCX Codex wrapper (kritical-codex.ps1). Leave blank to auto-discover in the workspace / extension. Used by the '✦ SCX Codex' button and the Open SCX Codex command.",
          "scope": "application"
        },
        "kritical.scxcode.modelCatalogPath": {
          "type": "string",
          "default": "C:\\KriticalSCX\\config\\models\\scx-model-catalog.json",
          "description": "Install-wide full model catalog JSON. Populated from SCX /v1/models, mirrored to ~/.kritical-scx/models-catalog.full.json, and used by the VS Code model picker plus SCX Codex wrapper.",
          "scope": "application"
        },
        "kritical.scxcode.defaultModel": {
          "type": "string",
          "default": "MiniMax-M2.7",
          "enum": [
            "MiniMax-M2.7",
            "MAGPiE",
            "gpt-oss-120b",
            "DeepSeek-V3.1",
            "Meta-Llama-3.3-70B-Instruct",
            "gemma-4-31B-it",
            "Qwen3-32B",
            "Llama-4-Maverick-17B-128E-Instruct",
            "coder"
          ],
          "enumDescriptions": [
            "230B sparse MoE (10B active) — default agentic pick, 192K ctx",
            "117B MoE from scx.ai — near o4-mini reasoning, 131K ctx",
            "117B open-weight MoE — cheapest reasoner, 131K ctx",
            "671B MoE (37B active) — reserve for hardest problems, 131K ctx",
            "70B dense — 405B-class perf, 131K ctx",
            "Google Gemma 4 31B — multimodal + thinking-mode toggle, 131K ctx",
            "Qwen3 32B dense — 119 languages, 32K ctx",
            "Llama 4 Maverick 400B MoE — multimodal text+image, 131K ctx",
            "SCX coder — highest context 196K, optimised for algorithms/debugging"
          ],
          "description": "Default model for chat + slash commands. Override per-message with /model or the picker."
        },
        "kritical.scxcode.autocompleteModel": {
          "type": "string",
          "default": "coder",
          "enum": [
            "coder",
            "MiniMax-M2.7",
            "gpt-oss-120b",
            "gemma-4-31B-it",
            "Qwen3-32B"
          ],
          "description": "Model for inline autocomplete. `coder` gives 196K ctx which fits large files."
        },
        "kritical.scxcode.fallbackChain": {
          "type": "array",
          "default": [
            "MiniMax-M2.7",
            "MAGPiE",
            "gpt-oss-120b"
          ],
          "items": {
            "type": "string"
          },
          "description": "Auto-failover chain on 429 / 5xx. Tries each in order. Empty array disables failover.",
          "markdownDescription": "Auto-failover chain on **429 / 5xx**. Tries each in order. Set to `[]` to disable failover (single-model mode)."
        },
        "kritical.scxcode.autocompact": {
          "type": "string",
          "default": "auto",
          "enum": [
            "off",
            "auto",
            "aggressive"
          ],
          "enumDescriptions": [
            "Never summarise — send full history until model context limit (may fail on long chats)",
            "Summarise oldest turns when context reaches 75% (recommended)",
            "Summarise more aggressively at 50% (better for long-lived chats)"
          ],
          "description": "How to compress chat history when the conversation approaches model context length."
        },
        "kritical.scxcode.systemPrompt": {
          "type": "string",
          "default": "You are a helpful coding assistant working inside VS Code. Prefer concise answers with code diffs. Never invent APIs — verify via docs first. When editing Kritical code, follow the HARD RULES in CLAUDE.md.",
          "description": "System message prepended to every chat.",
          "markdownDescription": "System message prepended to every chat. **Empty string disables** the default; be explicit."
        },
        "kritical.scxcode.telemetry": {
          "type": "string",
          "default": "off",
          "enum": [
            "off",
            "local-only",
            "kritical-endpoint"
          ],
          "description": "Cost + latency telemetry. `off` = zero collection. `local-only` = writes to Kritical.SCXCode.log in workspace. `kritical-endpoint` = also POSTs to Kritical telemetry endpoint (requires opt-in HKCU var).",
          "scope": "application"
        },
        "kritical.scxcode.storageBackend": {
          "type": "string",
          "default": "auto",
          "enum": [
            "auto",
            "sqlite",
            "mssql"
          ],
          "enumDescriptions": [
            "Use the local SQLite corpus when populated; otherwise try the SQL Server Express Lens store.",
            "Use only the local SQLite corpus store used by store-mcp/kritical-local-store.mjs.",
            "Use only SQL Server Express / KriticalSCXCodeStore Lens tables."
          ],
          "description": "Backing store used by Kritical Lens Looking Glass."
        },
        "kritical.scxcode.sqliteStorePath": {
          "type": "string",
          "default": "",
          "description": "SQLite corpus path. Empty resolves to KRIT_LOCAL_STORE or ~/.kritical-scx/scxcode-store.db."
        },
        "kritical.scxcode.mssqlServer": {
          "type": "string",
          "default": ".\\SQLEXPRESS",
          "description": "SQL Server instance for the SCXCode Lens big store."
        },
        "kritical.scxcode.mssqlDatabase": {
          "type": "string",
          "default": "KriticalSCXCodeStore",
          "description": "SQL Server database containing dbo.LensSource / dbo.LensCorpusFile / dbo.LensSymbol."
        },
        "kritical.scxcode.autoContext": {
          "type": "string",
          "default": "file+selection",
          "enum": [
            "off",
            "file",
            "file+selection",
            "workspace-tree"
          ],
          "enumDescriptions": [
            "No auto-context. Only the operator's prompt is sent.",
            "Include active file path + language + ± 30-line cursor window.",
            "Include file details + selection when non-empty (falls back to cursor window if no selection).",
            "Also include workspace root name (adds ~50 tokens)."
          ],
          "description": "How much editor state to inject as a system-prefix on every chat/slash command. Costs a few hundred tokens per turn."
        },
        "kritical.scxcode.autoContextMaxChars": {
          "type": "integer",
          "default": 8000,
          "minimum": 500,
          "maximum": 50000,
          "description": "Cap the auto-context code snippet at N characters (0.15 * tokens ratio → 8K chars ≈ 1.2K tokens)."
        },
        "kritical.scxcode.provider": {
          "type": "string",
          "default": "auto",
          "enum": [
            "auto",
            "scx-native",
            "claude-code-cli"
          ],
          "enumDescriptions": [
            "SCX first; on all-model + all-key 429, fall through to claude-code CLI on local machine (recommended).",
            "SCX only. Surfaces 429 as an error when quota is exhausted.",
            "claude-code CLI only. Never calls SCX. Uses your local claude session auth (no API key)."
          ],
          "description": "Which backend to chat through. `auto` gives you both."
        },
        "kritical.scxcode.claudeCliPath": {
          "type": "string",
          "default": "claude",
          "description": "Path to the claude-code CLI executable. Default `claude` assumes PATH. Set to full path if not on PATH."
        },
        "kritical.scxcode.maxTokens": {
          "type": "integer",
          "default": 1500,
          "minimum": 128,
          "maximum": 32000,
          "description": "Response length cap (max_tokens) for chat replies. Short≈800, Medium≈1500, Long≈4096, Maximum≈8192+. Higher = longer answers but slower / more cost.",
          "markdownDescription": "Response length cap (`max_tokens`) for chat replies. Guide: **Short** ≈ 800 · **Medium** ≈ 1500 · **Long** ≈ 4096 · **Maximum** ≈ 8192+. Higher = longer answers, slower, more cost."
        },
        "kritical.scxcode.concurrency": {
          "type": "integer",
          "default": 1,
          "minimum": 1,
          "maximum": 8,
          "description": "Synthetic-context concurrent streams (mux). 1 = single call. Higher fans out N concurrent SCX calls to widen effective context."
        },
        "kritical.scxcode.temperature": {
          "type": "number",
          "default": 0.2,
          "minimum": 0,
          "maximum": 1,
          "description": "Sampling temperature. 0 = deterministic, 1 = most creative."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "esbuild ./extension.ts --bundle --outfile=out/extension.js --external:vscode --format=cjs --platform=node",
    "watch": "esbuild ./extension.ts --bundle --outfile=out/extension.js --external:vscode --format=cjs --platform=node --watch",
    "test": "node ./out/test/runTests.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.5.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Sir-J-AU/scx-vscode.git"
  },
  "bugs": {
    "url": "https://github.com/Sir-J-AU/scx-vscode/issues"
  },
  "homepage": "https://kritical.net",
  "icon": "icon.png",
  "galleryBanner": {
    "color": "#13365C",
    "theme": "dark"
  },
  "extensionKind": [
    "ui"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": "limited",
      "description": "Extension reads SCX_API_KEY from HKCU env; safe. It does NOT execute untrusted workspace code. Chat + inline complete require trust because they can act on file contents."
    }
  },
  "dependencies": {
    "@iarna/toml": "^2.2.5"
  }
}
